name: KBOT-CICD

on: push

jobs:
  ci:
      name: CI
      runs-on: ubuntu-latest
      steps:
      - name: CheckOut
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Run test
        run: make test
        
